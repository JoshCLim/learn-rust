#![allow(unused)]

fn main() {
    /*
    most modern languages have some form of modularity
        i.e., multiple files contributing to the same program

    Cargo.toml defines a 'package'
    which contains one or more 'crates'
    which are a 'single compilable project' you can run

    src/main.rs is the entry point for the package
    src/lib.rs is the entry point for a library crate
    */
}
